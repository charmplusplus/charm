#!/bin/sh

if test ! -d "$1" 
then
 echo "$0 <directory>"
 exit 1
fi

rm -f bin include lib lib_so tmp

ln -s $1/bin bin
ln -s $1/include include
ln -s $1/lib lib
ln -s $1/lib_so lib_so
ln -s $1/tmp tmp
