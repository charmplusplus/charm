module LBDatabase {
  extern module NullLB;

  readonly CkGroupID lbdb;

  mainchare LBDBInit {
    entry LBDBInit(CkArgMsg *m);
  };

  group LBDatabase {
    entry void LBDatabase(void);  
  };

};
