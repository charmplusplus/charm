message ArrayCreateMessage;
message ArrayMessage;
message ArrayElementAckMessage;
varsize ArrayMigrateMessage;

group Array1D
{
  entry Array1D(ArrayCreateMessage *);
  entry RecvForElement(ArrayMessage *);
  entry RecvMigratedElement(ArrayMigrateMessage *);
  entry AckMigratedElement(ArrayElementAckMessage *);
};

