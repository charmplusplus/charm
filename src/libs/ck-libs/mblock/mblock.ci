mainmodule mblock {
  extern module MetisLB;
  readonly CkChareID _mainhandle;
  readonly CkArrayID _mblockaid;
  message DataMsg;
  message ChunkMsg;
  mainchare main {
    entry main();
    entry void done(void);
  };
  array [1D] chunk {
    entry chunk(ChunkMsg *);
    entry [threaded] void run(void);
    entry void recv(DataMsg *);
    entry void reductionResult(DataMsg *);
  };
};
