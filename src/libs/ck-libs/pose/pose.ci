/// Global POSE data and functions; includes and dependencies handled here
module pose {
  readonly double busyWait;
  readonly int POSE_endtime;
  message callBack;
  chare pose {
    entry pose();
    entry void IDon();
    entry void ETon();
    entry void start();
    entry void registerCallBack(callBack *);
    entry void stop();
    entry void exit();
  };
};
