
module Barrier 
{
  message DUMMY;
  message FP;

  group barrier {
    entry barrier(DUMMY *);
    entry void notify(DUMMY *);
    entry void callFP(DUMMY *);
    entry void reset(DUMMY *);
    entry void atBarrier(FP *);
  };
};
