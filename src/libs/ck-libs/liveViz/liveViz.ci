module liveViz {
message liveVizRequestMsg;
readonly liveVizConfig lv_config;
initcall void liveVizNodeInit(void);

/*group liveViz {
	entry liveViz(liveVizConfig cfg,CkArrayID A, CkCallback c);
};*/
}
