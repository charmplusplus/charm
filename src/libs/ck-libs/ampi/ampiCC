#!/bin/sh
CHARMBIN=`dirname $0`

while [ ! $# -eq 0 ]
do
  arg="$1"
  case "$arg" in
  -show)
     echo "charmc"
     exit 0
     ;;
  *) break
     ;;
  esac
  shift
done


ln $CHARMBIN/../include/mpi.h .
$CHARMBIN/charmc -language ampi $@
status=$?
/bin/rm -f mpi.h
exit $status
