mainmodule ampimain {
  extern module ampi;
  mainchare ampimain {
    readonly CkChareID handle;
    readonly ampi_comm_struct ampi_comms[8];
    readonly int ncomms;
    entry ampimain();
    entry void done(void);
    entry void checkpoint(void);
    entry void checkpointOnQd(void);
  };
}
