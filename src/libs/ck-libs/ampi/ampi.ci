module ampi {
  extern module MetisLB;
  message ArgsInfo;
  message AmpiMsg;
  message AmpiStartMsg;
  array[1D] ampi {
    entry ampi(AmpiStartMsg *);
    entry void generic(AmpiMsg *);
    entry [threaded] void run(ArgsInfo *);
    entry [threaded] void run(void);
    entry void migrate(void);
  };
}
