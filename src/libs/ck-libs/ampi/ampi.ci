module ampi {
  message AmpiMsg;
 
  initcall void ampiNodeInit(void);
  readonly ampi_comm_structs ampi_comms;
  readonly int ampi_ncomms;

  array[1D] ampi {
    entry ampi(int commidx,CProxy_TCharm threads_);
    entry void generic(AmpiMsg *);
  };
}
