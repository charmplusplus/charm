
module Barrier 
{
  message FP;

  group barrier {
    entry barrier(void);
    entry void notify(void);
    entry void callFP(void);
    entry void reset(void);
    entry void atBarrier(FP *);
  };
};
