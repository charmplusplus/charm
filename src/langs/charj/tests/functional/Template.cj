
class test {
      Array<int, 2> test[2:2, 4];
      Array<int, 2> test2 = new [2, 4](gen);
      Array<int> aaa[3];

      test() {
         aaa(abc, 4, test);
	 
	 def.init(abc, 4, test);

	 Array<double> ddd[5];
	 Array<double> ccc = new [1](get);

	 //test = new [2]();
      }
}
