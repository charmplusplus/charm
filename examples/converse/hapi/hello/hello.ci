mainmodule hello {
  readonly CProxy_Main main_proxy;
  readonly CProxy_Hello hello_proxy;
  readonly int n_elem;

  mainchare Main {
    entry Main(CkArgMsg* m);
    entry void done();
  };

  array [1D] Hello {
    entry Hello();
    entry void greet();
    entry void pass();
  };
};
