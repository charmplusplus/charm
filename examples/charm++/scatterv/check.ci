mainmodule check {

	readonly CProxy_Main mainProxy;
	readonly CProxy_Check checkArray;
	readonly int numchares;

	mainchare Main {
		entry Main(CkArgMsg* msg);
		entry [reductiontarget] void done();
	};

	array[1D] Check {
		entry Check();
		entry void scatterData();
		entry[scatterv] void recvScatter(scatter int arr[size], int size, double pos);
	};
};
