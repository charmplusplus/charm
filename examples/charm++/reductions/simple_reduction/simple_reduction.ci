mainmodule simple_reduction {
  readonly int units;
  readonly double dOne;
  readonly double dTwo;

  mainchare main {
    entry main(CkArgMsg *m);
    entry void reportIn(CkReductionMsg *msg);
  };

  array [1D] RedExample {
    entry RedExample(void);
    entry void dowork(void);	
  };        	
}
