mainmodule hello {
  readonly CProxy_Main mainProxy;

  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done(void);
  };

  nodegroup Hello {
    entry Hello(void);
    entry void SayHi(int hiNo);
  };
};
