mainmodule hello {
  readonly CProxy_Main mainProxy;
  readonly int nElements;

  mainchare Main {
    entry Main(CkArgMsg *);
    entry void done(void);
  };

  array [Fancy] Hello {
    entry Hello(void);
    entry void SayHi(int hiNo);
  };        	
};
