mainmodule hello {
  readonly CProxy_Main mainProxy;
  readonly int nElements;

  mainchare Main {
    entry Main(CkArgMsg*);
    entry void done();
  };

  array [Fancy] Hello {
    entry Hello();
    entry void SayHi(int hiNo);
  };
};
