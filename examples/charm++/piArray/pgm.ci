mainmodule pgm {

readonly CkChareID mainhandle;

mainchare main
{
  entry main(CkArgMsg *m);
  entry void results(int c);
};

array [1D] piPart
{
  entry piPart(void);
  entry void compute(int ns);
};

};
