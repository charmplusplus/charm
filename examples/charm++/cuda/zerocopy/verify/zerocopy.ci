mainmodule zerocopy {
  readonly CProxy_Main main_proxy;
  readonly CProxy_Block block_proxy;
  readonly int block_size;
  readonly int n_iters;

  mainchare Main {
    entry Main(CkArgMsg* m);
    entry [reductiontarget] void terminate();
  };

  group Block {
    entry Block();
    entry void init();
    entry void send();
    entry void receive(int size1, nocopydevice double arr1[size1], int size2,
        nocopypost double arr2[size2], int size3, int arr3[size3]);
  };
};
