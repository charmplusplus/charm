mainmodule hello {
  readonly CProxy_Main mainProxy;
  readonly int nElements;
  readonly CProxy_Hello arr;

  mainchare Main {
    entry Main(CkArgMsg* m);
    entry void done();
  };

  array [1D] Hello {
    entry Hello();
    entry void greet();
    entry void pass();
  };
};
