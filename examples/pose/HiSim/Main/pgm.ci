mainmodule Pgm {
  extern module BgSim;
  extern module TCsim;
  readonly CkChareID mainhandle;
  readonly int numWth;
  readonly unsigned int netLength;
  readonly unsigned int netHeight;
  readonly unsigned int netWidth;
  readonly Config config;
  mainchare main {
    entry main(CkArgMsg*);
  };
};
