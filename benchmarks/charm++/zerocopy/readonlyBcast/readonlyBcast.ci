mainmodule readonlyBcast {

  readonly std::vector<char> num_vec1;

  readonly CProxy_Main mProxy;

  mainchare Main{
    entry Main(CkArgMsg *msg);
    entry [reductiontarget] void done();
  };

  group group1 {
    entry group1();
  }
};
