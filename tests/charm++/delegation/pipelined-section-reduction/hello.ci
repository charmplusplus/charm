mainmodule hello {

  readonly CkChareID mid;
  readonly CProxy_Hello arr;
  readonly CkGroupID mCastGrpId;
  readonly int NumElements;
  readonly int CompleteMsgSize;
  readonly int NumFrags;
  readonly int NumReductions;
  readonly int SectionSize;


  message HiMsg {
    int data[];
  };

  mainchare main {
    entry main(CkArgMsg*);
    entry void maindone(void);
  };

  array [1D] Hello {
    entry Hello();
    entry void SayHi(HiMsg *);
    entry void start();
    entry void cb_client(CkReductionMsg *msg);
  };        	
};
