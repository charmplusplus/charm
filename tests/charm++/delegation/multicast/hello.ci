mainmodule hello {

  readonly CkChareID mid;
  readonly CProxy_Hello arr;
  readonly CkGroupID mCastGrpId;

  message HiMsg {
    int data[];
  };

  mainchare main {
    entry main();
    entry void maindone(void);
  };

  array [1D] Hello {
    entry Hello();
    entry void SayHi(HiMsg *);
    entry void start();
    entry void cb_client(CkSectionReductionMsg *msg);
  };        	
};
