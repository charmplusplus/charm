mainmodule migrate {
  readonly CProxy_Main mainProxy;
  readonly int nElements;

  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done(void);
  };

  array [1D] Hello {
    entry Hello(void);
    entry void SayHi(int hiNum);
    entry void ackPlease(int prev, int count);
};        	
};
