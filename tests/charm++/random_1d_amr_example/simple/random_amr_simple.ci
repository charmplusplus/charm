// Distributed under the MIT License.
// See LICENSE.txt for details.

mainmodule random_amr_simple {

  readonly CProxy_Main mainProxy;
  readonly CProxy_DgElement dgElementProxy;
  
  mainchare Main {
    entry Main(CkArgMsg* msg);
  };

  array [1D] DgElement {
    entry DgElement();
    entry [reductiontarget] void iterate(int nelements);
    entry void split();
    entry void join();
    entry void init_child(int itercount_);
    entry void init_parent(int itercount_);
    entry [reductiontarget] void delete_old(int nelements);
  };
};
