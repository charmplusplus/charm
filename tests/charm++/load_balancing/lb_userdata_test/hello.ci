mainmodule hello {
  readonly CProxy_Main mainProxy;
  readonly int nElements;
  readonly int nSteps;
  readonly int lbstep;

  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done(void);
  };

  array [1D] Hello {
    entry Hello(void);
    entry void doWork();
  };        	
};
