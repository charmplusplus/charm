mainmodule migmanifest {
  readonly CProxy_Main mainProxy;
  readonly int nElems;

  mainchare Main {
    entry Main(CkArgMsg*);
    entry void done();
  };

  array [1D] Elem {
    entry Elem();
    entry void iterate();
  };
};
